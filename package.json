{
    "name": "smartcms-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "cd frontend && npm run dev",
        "install:all": "npm install && cd frontend && npm install"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}